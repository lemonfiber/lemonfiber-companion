{
    "name": "modules/vault",
    "description": "Secure storage and the app lock.",
    "license": "LicenseRef-Hippocratic-3.0",
    "type": "library",
    "require": {
        "php": "^8.5",
        "modules/kernel": "dev-main",
        "nativephp/mobile": "^4.4"
    },
    "autoload": {
        "psr-4": {
            "Modules\\Vault\\": "src/",
            "Modules\\Vault\\Tests\\": "tests/"
        }
    },
    "extra": {
        "lemonfiber": {
            "floors": {
                "coverage": 100,
                "mutation": 0,
                "mutation-is-zero-because": "Three adapters over the device's store, each held by its port's contract — SecureStorage, Stacks and Verdicts — run against the shipped adapter and the fake over one set of assertions, and by `PlatformStacksTest` and `PlatformVerdictsTest` over the keys a stack gets and the JSON a row is written as."
            },
            "kind": "adapter"
        }
    }
}
