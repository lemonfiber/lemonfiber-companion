<?php

declare(strict_types=1);

namespace Modules\Operator\Internal;

/**
 * One sentence carried out of the arm a listing took.
 *
 * `WhatRunsUnattended::whereItCannot()` answers an object because both arms
 * must build something (`C2`), and a string is not one. It exists so the fold
 * has something to carry and nothing else holds it — which is why it is here
 * rather than in `ViewModels`, where everything is a shape a template reads.
 */
final readonly class WhatTheMachineSaysInstead
{
    public function __construct(public string $said) {}
}
