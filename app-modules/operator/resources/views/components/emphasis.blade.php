{{-- The line carrying the weight in a group of them — a service's name, a
     count, an obstacle. Weight rather than colour, because colour here would
     assert a token where `DES-R24` maps a role. --}}
<native:text class="font-bold">{{ $slot }}</native:text>
