{{-- The line that names what follows it. `text-lg` is the platform's own
     larger role and scales with whatever the reader has set (`DES-R25`). --}}
<native:text class="text-lg font-bold">{{ $slot }}</native:text>
