{{-- One thing in a list of them, with what is said about it stacked under its
     name. The gap is the platform's smallest, which is what makes the lines
     read as one entry rather than as a list of their own.

     Opened before the slot and closed by `entry-closes` after it, so the slot
     is drawn inside this column. --}}
<native:column class="w-full gap-1">
