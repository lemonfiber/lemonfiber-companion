# Link checking, for the hygiene gate the spec repo defines (Q-R54).
#
# The gate runs `lychee .` over this tree, and lychee skips hidden directories
# unless told otherwise. The decision records live under `.docs/`, so without
# this their links are not checked — and a link check that quietly checks
# nothing is the failure every rule in this repository exists to prevent.
#
# `.git` is excluded because turning hidden files on lets lychee walk it, and
# what it holds is compressed objects rather than documents.
hidden = true
exclude_path = [".git"]

# Retries, because a link check that fails on one slow host teaches people to
# re-run it rather than to read it.
max_retries = 2

# 206 is a partial response and 429 is a host asking to be asked less often.
# Neither says the link is wrong.
accept = ["200", "206", "429"]
exclude_loopback = true
